## 7월
### 14일
- fdx 백엔드, 프론트엔드 리드미 클로드로 최신화 
- 오라클 프리티어 인스턴스 생성 실패
  - git action이나 오라클 작은 인스턴스로 매크로 돌려야되나 고민
- aws lightsail 소액으로 돌릴까 고민
  - 종량제가 아니라 월정액으로 되는지 확실히 확인하자
  - 안되면 다른 vps찾자
### 16일
- 오라클 인스턴스 1OCUP/6GB 생성 자동화
- SSH : Secure Shell. 원격으로 서버 컴퓨터 명령창에 접속. 개인키를 써서 secure임
### 18일
- 오라클 인스턴스 생성 자동화 복습
  - SSH로 오라클 E2 서버에 접속
  - E2 서버에 OCI CLI 설치
  - OCI CLI를 내 오라클 클라우드 계정과 연결하여 개인키, 공개키 생성
  - 오라클 콘솔에 위 공개키를 등록하여, E2에서 개인키로 서명해 보낸 요청을 받게 함
  - E2에서 오라클 클라우드에 A1 생성요청하는 스크립트 작성
  - (스크립트 : 명령어가 순서대로 있는 파일. 실행하면 명령어가 순서대로 실행됨)
  - cron이 일정한 시간마다 스크립트를 실행하도록 함 
### 19일
- codex가 다운로드폴더 읽기권한 가져서 ssh key 쓰는거 보안 거부당함
  - 그래서 kjh폴더에 복사해서 썻더니 됨 
### 20일
- aws lightsail 인스턴스 생성
  - 리눅스, 우분투 24.04 LTS
  - Size: $12 / 2GB RAM / 2 vCPU / 60GB SSD
- 인스턴스에 고정 IP 연결
  - 인스턴스 중지햇다 켜서 IP 바뀌면 GitHub Secrets, OAuth 리다이렉트 URI를 전부 다시 고쳐야 하기 때문
- Connect using SSH연결 (브라우저로 키파일 없이)
  - 서버 업데이트
  - docker 설치
  - docker compose 설치
  - 2GB 서버용 스왑 추가
    - Spring Boot와 MySQL을 함께 실행하므로 2GB 스왑을 만들어 두는 게 안전
    - 랩 부족할때 덜 중요한 걸 SSD 스왑 영역으로 보내버림 
- 요금폭탄 방지
  - aws에서 예산 설정하고 비용 어느정도 차면 메일발송하게 함 
