# 가장 빠른 길 찾기
- 최단 경로는 상황에 맞는 효율적 알고리즘이 이미 정립되어 있다
  - ex) 한 지점에서 다른 특정 지점까지 최단 거리
  - ex) 모든 지점에서 다른 모든 지점까지 최단 거리
- 최단 경로 문제는 보통 그래프를 통해 출제
- <img width="304" height="193" alt="image" src="https://github.com/user-attachments/assets/d4adcce8-963f-410d-86d8-26d89d13e593" />

  - 노드
  - 간선
  - 가중치
- 학부 수준 최단거리 3대 알고리즘
  - 다익스트라
  - 플로이드 워셜
  - 벨만포드

| 알고리즘 | 특징 | 알고리즘 유형 |
| :--- | :--- | :--- |
| **다익스트라 (Dijkstra)** | **한 지점**에서 다른 모든 지점까지의 최단 거리를 구할 때 사용 | **그리디 (Greedy)** |
| **플로이드 워셜 (Floyd-Warshall)** | **모든 지점**에서 다른 모든 지점까지의 최단 거리를 모두 구할 때 사용 | **다이나믹 프로그래밍 (DP)** |
| **벨만 포드 (Bellman-Ford)** | 간선의 가중치가 **음수**일 때도 사용 가능 (이 책에선 생략됨) | - |
# 다익스트라 (Dijkstra)
> 특정 노드에서 출발하여 다른 모든 노드로 가는 각각의 최단 경로를 구하는 알고리즘
- **특징:** '음의 간선'(0보다 작은 값을 가지는 간선)이 없을 때 정상적으로 동작합니다.
- **유형:** 매번 '가장 비용이 적은 노드'를 선택하므로 **그리디(Greedy) 알고리즘**으로 분류됩니다.
## 동작 원리 (6단계)
1. 출발 노드 설정: 시작 지점을 정합니다.
2. 최단 거리 테이블 초기화: 출발 노드는 0, 나머지 노드는 무한(INF)으로 설정합니다.
3. 노드 선택: 방문하지 않은 노드 중에서 최단 거리가 가장 짧은 노드를 선택합니다.
4. 테이블 갱신: 해당 노드를 거쳐 다른 노드로 가는 비용을 계산하여 기존 값보다 작으면 교체합니다.
5. 반복: 모든 노드를 방문할 때까지 3번과 4번 과정을 반복합니다.
- 최단 거리 테이블 : 각 노드에 대한 현재까지의 최단 거리
